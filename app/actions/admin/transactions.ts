/**
 * @deprecated Sử dụng '@/app/actions/admin/donations' thay thế.
 * File này tồn tại để duy trì backward compatibility.
 */
export {
    confirmTransaction,
    cancelTransaction,
    updateTransactionStatus,
    revokeTransaction,
    deleteTransaction,
} from './donations';
